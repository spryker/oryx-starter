import { labsFeatures } from "@spryker-oryx/labs";

// we need to install this package in order to have it working: `npm i @spryker-oryx/labs`
// in order to see transparent images, we need to provide a Cloudinary ID.

export const labs = labsFeatures;
